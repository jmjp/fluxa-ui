{
  "name": "@fluxa/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Tipos e utilitários compartilhados entre Agent UI e Admin UI",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings=0",
    "test": "echo 'no tests yet' && exit 0"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
